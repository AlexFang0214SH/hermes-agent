- 在 Docker 引导脚本中实施严格的权限隔离策略，仅在受控子目录（如 cron, sessions）执行递归 chown，避免破坏主机挂载卷的所有权。
- 安装脚本遵循“托管优先”原则，将核心工具链（如 uv, Node.js）安装至用户级目录（$HERMES_HOME），以避免与系统级包管理器冲突并确保非 root 用户可写。
- Nix 模块采用深度合并（deep merge）策略处理 declarative settings，确保用户自定义配置与 NixOS 声明式配置在激活时能正确叠加而非覆盖。