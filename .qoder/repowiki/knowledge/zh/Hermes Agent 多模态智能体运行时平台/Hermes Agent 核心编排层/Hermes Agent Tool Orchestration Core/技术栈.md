Python `asyncio` with persistent event loops (`_get_tool_loop`, `_get_worker_loop`) to bridge synchronous dispatchers with async tool handlers and prevent 'Event loop is closed' errors in long-lived processes.