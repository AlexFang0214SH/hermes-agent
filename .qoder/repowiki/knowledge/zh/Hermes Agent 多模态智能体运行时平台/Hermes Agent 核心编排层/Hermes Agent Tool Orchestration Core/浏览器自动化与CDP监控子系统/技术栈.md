- **底层驱动**：依赖 `agent-browser` CLI (Playwright/Chromium) 或 `camofox-browser` (Node.js + Camoufox)。
- **通信协议**：使用 `websockets` 库建立原生 Chrome DevTools Protocol (CDP) 长连接进行实时监控；使用 `requests` 与 Camofox REST API 交互。
- **异步桥接**：通过 `asyncio.run_coroutine_threadsafe` 在同步工具调用与异步 CDP 事件循环之间建立安全桥接。