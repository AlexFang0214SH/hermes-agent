- 所有MCP子进程的stderr被重定向至共享日志文件 (`~/.hermes/logs/mcp-stderr.log`)，以防止污染TUI显示并保留调试信息。
- 工具调用处理器统一实现电路断路器模式，连续失败达到阈值后进入冷却期，避免模型在服务器不可用时陷入重试循环。
- 错误消息在返回给LLM前必须经过 `_sanitize_error` 处理，使用正则表达式剥离GitHub PAT、Bearer Token等敏感凭证信息。
- 环境变量的传递遵循最小权限原则，仅允许白名单内的基础变量（如PATH, HOME）及用户显式配置的变量传递给stdio子进程。