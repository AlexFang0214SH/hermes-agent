- 所有工具模块必须在导入时调用 `registry.register()` 完成自注册，禁止在外部维护并行工具列表。
- 插件中的懒加载单例必须使用 `plugins/plugin_utils.py` 提供的 `lazy_singleton` 或 `SingletonSlot` 以确保线程安全。
- 网关适配器需实现统一的 `send` 与 `handle_event` 接口，并通过 `platform_registry` 进行动态加载。