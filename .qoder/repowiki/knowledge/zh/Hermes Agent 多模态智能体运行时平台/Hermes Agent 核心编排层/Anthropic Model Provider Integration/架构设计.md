- **插件化架构**：遵循 `plugins/model-providers` 目录规范，通过 `plugin.yaml` 声明元数据，`__init__.py` 执行逻辑。
- **注册机制**：实例化 `AnthropicProfile`（继承自 `providers.base.ProviderProfile`）并调用 `register_provider` 将其加入全局提供者注册表。
- **接口实现**：重写 `fetch_models` 方法，使用 `urllib.request` 直接调用 Anthropic API (`/v1/models`)，并处理特有的 `x-api-key` 和 `anthropic-version` 请求头。
- **配置映射**：定义环境变量映射（如 `ANTHROPIC_API_KEY`）、默认辅助模型及 API 模式 (`anthropic_messages`)。