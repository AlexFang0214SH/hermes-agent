- 所有模型提供者插件必须在 `__init__.py` 中实例化 `ProviderProfile` 子类并立即调用 `register_provider` 完成自注册。
- 提供者配置需明确指定 `env_vars` 元组以支持多源密钥加载（如 `ANTHROPIC_API_KEY`, `CLAUDE_CODE_OAUTH_TOKEN`）。
- 自定义 `fetch_models` 实现需包含完善的异常捕获并返回 `None` 或模型 ID 列表，确保在 API 不可用时优雅降级。