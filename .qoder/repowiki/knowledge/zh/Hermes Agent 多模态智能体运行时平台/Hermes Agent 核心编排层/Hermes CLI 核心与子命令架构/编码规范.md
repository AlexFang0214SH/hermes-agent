- 子命令解析器采用工厂函数模式（`build_<name>_parser`），接收 `subparsers` 对象并注入对应的处理函数（`cmd_*`），以避免循环导入。
- 在模块导入阶段执行平台兼容性检查与环境修复（如 `_ensure_utf8` 和 Termux 快速路径），确保后续逻辑运行的稳定性。
- 敏感操作或交互式命令（如 `setup`, `tools`）在执行前通过 `_require_tty` 检查标准输入是否为终端，防止在非交互环境下意外挂起。