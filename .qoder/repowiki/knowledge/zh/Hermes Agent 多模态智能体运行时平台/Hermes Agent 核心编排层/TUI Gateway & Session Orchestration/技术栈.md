- **通信协议**：JSON-RPC 2.0 over Stdio/WebSocket。
- **并发模型**：基于 `concurrent.futures.ThreadPoolExecutor` 的异步 RPC 分发，配合 `contextvars` 维持上下文。
- **进程间通信**：通过 `subprocess.Popen` 管理持久的 `slash_worker` 子进程，使用队列进行 stdout/stderr 排水。