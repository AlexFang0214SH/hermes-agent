- 所有面向用户的文本输出必须路由至 stderr，严禁污染用于 ACP JSON-RPC 通信的 stdout。
- 异步回调（如权限请求、会话更新）通过 `safe_schedule_threadsafe` 从工作线程安全地调度到主事件循环执行。
- 工具调用结果在发送给客户端前需经过结构化格式化（如 Markdown 表格、代码块），并实施长度截断以防止 UI 溢出。
- 会话状态变更（如模型切换、历史更新）必须同步触发数据库持久化，确保进程重启后可恢复。