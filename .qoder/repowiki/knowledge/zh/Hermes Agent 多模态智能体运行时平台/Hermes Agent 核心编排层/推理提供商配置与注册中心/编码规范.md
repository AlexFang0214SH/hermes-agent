- 采用声明式配置模式，将提供商的行为特征（如端点、鉴权方式、消息预处理逻辑）集中在 `ProviderProfile` 数据类中定义，避免在传输层分散处理布尔标志。
- 遵循最后写入优先（Last-writer-wins）的注册覆盖规则，允许用户通过 `$HERMES_HOME` 下的插件目录无缝覆盖或替换内置的提供商配置。
- 使用钩子方法（Hook Methods）处理差异化逻辑，如 `build_api_kwargs_extras` 用于区分不同提供商对推理参数（顶层参数 vs extra_body）的不同要求。