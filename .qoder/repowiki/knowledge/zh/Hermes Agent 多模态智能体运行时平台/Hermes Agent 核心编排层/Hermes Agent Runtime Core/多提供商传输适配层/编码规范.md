- 所有传输实现必须继承 `ProviderTransport` 基类并重写 `convert_messages`、`build_kwargs` 和 `normalize_response` 核心方法。
- 提供商特定的元数据（如 Codex 的 `response_item_id` 或 Anthropic 的 `thinking` 块）必须封装在 `provider_data` 字典中，严禁污染顶层共享字段。
- 在将消息发送给严格遵循规范的提供商前，必须深度复制并清洗掉内部脚手架字段（如下划线前缀键或非标准 `tool_name`）。