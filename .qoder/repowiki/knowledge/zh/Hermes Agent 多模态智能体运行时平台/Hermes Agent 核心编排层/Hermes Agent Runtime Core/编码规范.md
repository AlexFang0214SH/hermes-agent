- Lazy module references via `_ra()` function to preserve test patching contracts on `run_agent` symbols
- Use of `dataclass` with `extra` dict for flexible credential payload handling in `PooledCredential`
- Thread-safe state management using `threading.RLock` or `threading.Lock` for shared agent attributes like `_tool_worker_threads` and `_pending_steer`