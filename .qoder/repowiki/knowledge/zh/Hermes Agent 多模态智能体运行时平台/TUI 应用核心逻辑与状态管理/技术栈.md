- **状态管理**：使用 `nanostores` (atom/computed) 替代 Redux/Zustand，以优化 React 渲染性能并支持非 React 上下文访问。
- **终端 UI**：基于 `@hermes/ink` (Ink 的分支或定制版) 构建 React-based TUI。
- **通信协议**：通过 `GatewayClient` 使用 JSON-RPC over WebSocket 与 Python 后端通信。