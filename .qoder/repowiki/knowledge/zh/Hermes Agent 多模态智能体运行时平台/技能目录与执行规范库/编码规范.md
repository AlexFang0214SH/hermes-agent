- 所有技能入口文件必须命名为 `SKILL.md` 并包含 YAML 格式的元数据头（name, description, version, platforms）。
- 执行脚本需实现自动化的环境探测与依赖安装逻辑（如 `install_deps` 函数），并在失败时提供明确的修复指令。
- 涉及第三方服务认证的脚本必须支持非交互式 OAuth 流程或令牌注入，并将凭证统一存储在 `$HERMES_HOME` 目录下。
- 工具类脚本的输出必须为标准 JSON 格式，且包含 `status` 字段以明确标识执行成功或失败的状态。