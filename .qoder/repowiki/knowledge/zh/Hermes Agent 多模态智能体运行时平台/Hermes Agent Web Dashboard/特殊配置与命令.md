- **开发环境同步**：Vite 配置了自定义插件 `hermesDevToken`，在开发模式下自动从后端 `http://127.0.0.1:9119` 抓取 Session Token 并注入 HTML，确保本地开发时的 API 鉴权通过。
- **代理配置**：`vite.config.ts` 中配置了 `/api` 和 `/dashboard-plugins` 的代理，将请求转发至后端网关服务。
- **构建输出**：生产构建产物直接输出至 `../hermes_cli/web_dist` 目录，以便 Python CLI 工具直接托管静态资源。