- 文档文件统一采用 kebab-case 命名规范（如 `adding-providers.md`），并与侧边栏配置中的路径标识符保持一致。
- 每个文档分类目录下均包含 `_category_.json` 文件，用于定义该分类在侧边栏中的显示标签、位置及默认折叠状态。
- 技能文档按功能域（如 apple, creative, devops）和来源（bundled/optional）进行深层目录嵌套，路径结构直接映射到侧边栏的层级关系。